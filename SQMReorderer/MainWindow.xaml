<Window x:Class="SQMReorderer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <HierarchicalDataTemplate x:Key="groupTemplate" ItemsSource="{Binding Vehicles}">
            <StackPanel>
                <TextBlock Text="{Binding Side}" />
                <!--<ContentPresenter />-->
            </StackPanel>
        </HierarchicalDataTemplate>
    </Window.Resources>
    
    <Grid>
        <TreeView ItemsSource="{Binding Groups}" ItemTemplate="{StaticResource groupTemplate}" />
    </Grid>
</Window>
